#!/usr/bin/env python3

hostname = input('please put in your hostname: ')
if hostname.upper() == 'MTG':
  print('The hostname matches expected config')
  print('this is another line')
print('exiting the script')
